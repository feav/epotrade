{% extends 'base.html.twig' %}

{% block title %}Signaux{% endblock %}

{% block body %}
    <h1>Signaux</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ signaux.id }}</td>
            </tr>
            <tr>
                <th>Date_placement</th>
                <td>{{ signaux.datePlacement ? signaux.datePlacement|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Evenement</th>
                <td>{{ signaux.evenement }}</td>
            </tr>
            <tr>
                <th>Gain</th>
                <td>{{ signaux.gain }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ signaux.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('signaux_index') }}">back to list</a>

    <a href="{{ path('signaux_edit', {'id': signaux.id}) }}">edit</a>

    {{ include('signaux/_delete_form.html.twig') }}
{% endblock %}
