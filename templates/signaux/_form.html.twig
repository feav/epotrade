<style type="text/css">
    button[type=submit]{float: right;}
</style>
<div class="col-sm-6 col-sm-offset-3" style="margin:auto">
	{{ form_start(form) }}	
		<div class="label label-error">{{ form_errors(form) }}</div>
		    <div class="form-group mb-4">
		        <label>Evenement</label>
		        {{ form_widget(form.evenement, {'attr': {'class': 'form-control'}}) }}
		    </div>
		    <div class="form-group mb-4">
		        <label>Gain (€)</label>
		        {{ form_widget(form.gain, {'attr': {'class': 'form-control'}}) }}
		    </div>
		    <div class="form-group mb-4">
		        <label>Status</label>
		        {{ form_widget(form.status, {'attr': {'class': 'form-control'}}) }}
		    </div>
		{{ form_widget(form._token) }}
	    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
	{{ form_end(form) }}
</div>
